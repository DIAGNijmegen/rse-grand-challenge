You can upload your method's ground truth separately from the container image as a tarball (.tar.gz) on Grand Challenge (Phase settings > Ground Truths).

If provided, the tarball will be extracted to `/opt/ml/input/data/ground_truth/` at runtime.
